# Triage: issue #11 — AFM CCDII guidance page moved (URL dead) + new "doorlopende verplichtingen" page

- **Issue:** https://github.com/frankdevlabs/ccd2-2023-2225/issues/11
- **Source:** AFM — CCDII sector page (`T1-07`) — https://www.afm.nl/nl-nl/sector/kredietaanbieders/4-herziene-richtlijn-consumentenkrediet-ccdii
- **Detected:** 2026-06-18
- **Type / playbook:** institutional / institutional

## What was flagged

The watched AFM CCDII sector page now persistently returns a 404 ("Error 404: pagina niet gevonden")
at the tracked URL. AFM restructured its CCDII guidance: the page **moved** (dropped the `4-` URL prefix)
and a **new sub-page** was added.

Confirmed by fetching the live site:
- **Dead:** `…/kredietaanbieders/4-herziene-richtlijn-consumentenkrediet-ccdii` → 404.
- **Moved to:** `…/kredietaanbieders/herziene-richtlijn-consumentenkrediet-ccdii` → live, title
  "Herziene richtlijn consumentenkrediet CCDII", same substance as before (scope expansion to BNPL /
  deferred-debit / lease-with-option, licence applications accepted ahead of 20 Nov 2026, responsible
  lending + age verification).
- **New sub-page:** "CCDII: drie doorlopende verplichtingen" at
  `…/kredietaanbieders/ccdii--doorlopende-verplichtingen` → live. Sets out three ongoing obligations:
  1. **Responsible lending / passend krediet** — proportional creditworthiness; for short-term credit
     (repayment ≤ 3 months) verify age **and** consult BKR, assess financial position from verified
     information, refuse if not passend ("de kredietverstrekker verstrekt geen krediet als dat niet
     passend is"), and run an update policy for ongoing (doorlopend) credit.
  2. **Age verification** — "aan de hand van een betrouwbare bron, zoals iDIN".
  3. **Preventive management / early signalling** — detect consumers in difficulty and refer to debt
     advice; AFM will update its "Consumenten en Incassotrajecten" guidance.

## Assessment

**Substantive and in-scope.** This is the AFM CCDII sector page (`file_id` = Directive (EU) 2023/2225
NL transposition), not a `keep_apart` sibling. The flag is two things at once:
- a **source-health** problem (the watched URL is dead → fix `tracker.yaml`), and
- a small **content** addition (the new "doorlopende verplichtingen" sub-page makes explicit the
  short-term-credit ≤3-month age+BKR rule and the update-policy duty, which the repo only covered
  generically). The old substance is unchanged — most of it is already in `supervision-afm.md`.

## Plan

1. **`tracker.yaml`** (`T1-07.url`) — replace the dead URL with the moved page
   `…/kredietaanbieders/herziene-richtlijn-consumentenkrediet-ccdii` so the daily tracker stops 404ing.
   (Source-health fix; not in `affects` but required to clear the flag.)
2. **`docs/transposition/supervision-afm.md`** — update the CCDII-page URL (dead → moved); add a short
   sub-section capturing the new "drie doorlopende verplichtingen" page (the ≤3-month short-term-credit
   age+BKR + passend rule, the update policy for doorlopend krediet) with its link.
3. **`STATUS.md`** (AFM block, "Latest act") — update the dead AFM CCDII link to the moved URL and note
   the new "doorlopende verplichtingen" guidance.

## Verification

- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` ends "0 broken".
- Read `docs/transposition/supervision-afm.md` and `STATUS.md` AFM block — URLs point to the moved page;
  new sub-page summarised.
- Close #11 once merged.
